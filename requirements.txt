requests>=2.32
Pillow>=11.0
python-dotenv>=1.0
json-repair>=0.30
flask>=3.0
gunicorn>=22.0
pytest>=8.0
markdown>=3.0

# RAG (опционально). Индекс (data/rag/*) уже собран и закоммичен; без этих пакетов
# retrieval работает по тегам (точное совпадение цветотип/фигура/поле). Установи их,
# чтобы включить семантическое ранжирование эмбеддингами (локально или в проде):
#   pip install fastembed numpy   и пересобрать:  python -m scripts.build_rag_index
# fastembed>=0.8
# numpy>=2.0
# детекция лица для identity-preserving рендера (YuNet). <5: в OpenCV 5 нет CascadeClassifier,
# а нам важна стабильность API dnn/FaceDetectorYN
opencv-python-headless<5
