# План: MVP-срез для конкурса (вертикальный пайплайн)

**Цель:** запускаемый MVP в публичном репозитории к 20 июля 2026 (3-я волна Junior ML Contest).
**Темп:** 5 ч/день. Окно: 25 июня – 20 июля (подача), защита 27–29 июля.
**Связан с:** [2026-06-25-itmo-jmlc-zayavka.md](2026-06-25-itmo-jmlc-zayavka.md)

## Что строим — один сквозной сценарий

```
квиз Identity Scan → Gap-профиль
   + фото вещи
        → vision-analyzer.md   → Claude Vision (анализ вещи)
        → formula-diagnostic.md → диагностика стиля (Формула стиля)
        → look-generator.md    → генерация капсулы (изображение)
        → результат на экране + Identity Gap %
```

Промпты уже готовы в `architecture/prompts/` — MVP это обёртка вокруг них, а не разработка с нуля.

## Стек (под критерии «Инженерия» + «DS»)

**Решение (2026-06-25):** конкурсный MVP строим на **Python** (не Next.js из CLAUDE.md).
Причина — жюри сильнее всего смотрит на DS и инженерию ML-пайплайна, промпты уже
написаны под Python, eval на Python собирается естественно. Next.js-продукт по
CLAUDE.md — после конкурса. Отклонение зафиксировать в mvp-spec.

- **AI-слой:** пакет `core/` — загрузка промптов из `architecture/prompts/`, клиент
  OpenRouter (OpenAI-совместимый), vision + диагностика. Фото сжимаются до 1024px.
- **Eval (Python):** `eval/` — ground-truth + метрики (accuracy Vision vs эксперт,
  табу-цвета, A/B генераторов).
- **Фронт:** лёгкий — переиспользуем HTML-квиз + простая страница результата (FastAPI/статика).
- **Инфра:** Docker, GitHub Actions CI (pytest), README с архитектурой и метриками.

## Структура репозитория (реализовано)

```
core/
  config.py     — маршрутизация моделей по тирам (dev/final), ключ из env
  prompts.py    — загрузчик системных промптов (2 формата) + кэш
  provider.py   — клиент OpenRouter, vision, сжатие фото, парсинг JSON
  pipeline.py   — vision → диагностика → (капсула)
scripts/run_vision.py — CLI первого живого вызова
tests/          — pytest загрузчика (без API), зелёные
eval/           — метрики (Фаза 3)
Dockerfile, .dockerignore, .github/workflows/ci.yml, README.md, .env.example
```

## Фазы

### Фаза 1. Каркас + ядро пайплайна [~] (Неделя 1: 25 июня–1 июля)
- [x] Каркас репо: `core/`, env-конфиг, Docker, GitHub Actions CI, README, тесты (зелёные).
- [x] Приватный GitHub-репозиторий создан и запушен: github.com/neiroskyai-jpg/sense-style-ai_ (секреты и перс. данные исключены, проверено).
- [x] Безопасная настройка ключа: Windows env var `OPENROUTER_API_KEY`, в файлах открытым текстом нет.
- [x] `core`: загрузчик промптов (2 формата), клиент OpenRouter, сжатие фото.
- [x] Пайплайн: функции `analyze_photos` (vision-analyzer) и `diagnose` (formula-diagnostic) написаны и стыкуются.
- [x] **Сквозная цепочка vision → диагностика проверена вживую** (CLI `run_pipeline`): на «01-do» → Identity Gap 78%, Формула «Минимализм × Power Woman × Drama-акцент», visual_formula корректна по методологии (прямоугольник → визуальная талия, не футляр).
- [x] Баг-фикс: загрузчик промптов обрезал `formula-diagnostic` (591 из 22230 символов) по внутренним `## `-подзаголовкам. Починен, добавлен регресс-тест.
- [x] **Первый ЖИВОЙ вызов Vision** прошёл (Gemini Flash, dev): ключ/баланс/слаг ок, валидный JSON. На фото «01-do» определил `winter_natural` (high) — совпадает с эталонной разметкой кейса Ксении.
- [ ] **Параллельно (Ксения): старт разметки ground truth + выход на клиенток за отзывами.**

### Фаза 2. Полный вертикальный срез [~] (Неделя 2: 2–8 июля)
- [x] `generate_capsule` (look-generator + подклеенный style-library): Формула → капсула вещей + образы с `image_generation_prompt`.
- [x] `generate_image`: генерация образа через OpenRouter. **Seedream на OpenRouter недоступна** (в каталоге только текстовые `bytedance-seed/*`) → основная Nano Banana (`google/gemini-2.5-flash-image`), A/B с `gemini-3-pro-image`.
- [x] Устойчивый JSON-парсинг (ретраи + json-repair) — LLM изредка отдаёт битый JSON.
- [x] **Сквозной прогон end-to-end вживую:** фото «01-do» → Gap 75% → капсула (9 вещей/24 комбинации/6 образов) → сгенерированный образ, визуально точно по диагностике (пеплум для прямоугольника, палитра графит/молочный/рубин).
- [x] Ветка `feat/look-generator` запушена; `/security-review` по диффу — 0 находок (код чистый). PR: github.com/neiroskyai-jpg/sense-style-ai_/pull/new/feat/look-generator
- [x] Identity-preserving рендер (Gemini 3 Pro i2i): клиентка в образах под сценарии, лицо/фигура держатся (GPT отпал — refusal на реальные лица).
- [x] **Фронт (Flask, `app/main.py`):** форма (фото + квиз) → пайплайн → страница результата с Identity Gap %, Формулой и образами клиентки. Сквозной веб-тест: Gap 67% + 2 образа. Загрузка фото защищена (лимит 15 МБ, проверка формата, MAX_IMAGE_PIXELS от decompression bomb). HTML-квиз Identity Scan со скорингом — можно подключить позже вместо простой формы.

### Фаза 3. Eval, метрики, отзывы [~] (Неделя 3: 9–15 июля)
- [x] **LLM-судья** (`evaluation/judge.py`): метрики образа vs диагностика (палитра/акцент/figure_fit/identity/overall). Демо: различил figure_fit 0.8 (уплывший) vs 1.0. Оговорка: дешёвый судья снисходителен к лицу → на финал брать Claude.
- [ ] Eval-харнесс на N кейсах: accuracy Vision vs **экспертная разметка Ксении** (нужен ground truth), табу-цвета, A/B генераторов → агрегированный отчёт.
- [x] Метрика Identity Gap «до/после» во времени — трекинг (`core/tracking.py`, SQLite), показывается в вебе.
- [x] **Доп. фичи:** шоп-лист + бюджет (`generate_shopping_list`); агентный self-correction (судья + авто-ретрай рендера).
- [ ] 3–5 отзывов клиенток + 2–3 кейса «до/после».
- [ ] README с метриками, чистка кода.

### Фаза 4. Документы + подача [ ] (Неделя 4: 16–20 июля)
- [ ] Описание проекта (PDF, 3 стр.), презентация (шаблон), CV, мотивационное письмо.
- [ ] Загрузить всё в личный кабинет до 20 июля.

### Фаза 5. Защита [ ] (21–29 июля)
- [ ] Репетиция (5 мин + ответы жюри), демо MVP вживую.

## Идеи на добор «wow» (не сделано)

- Style Book PDF (промпт `style-book` + навык pdf) — осязаемый артефакт.
- Гардероб клиентки (vision её реальных вещей в БД → капсула из того, что есть).
- «До/после» слайдер (фото ↔ образ) — визуализация закрытия Gap для питча.

## Развёрнуто (РФ, комплаентно)

**Живой бэкенд на Amvera Cloud (РФ):** https://sense-style-kseniiak.amvera.io/ — форма
(фото + квиз + согласия) → диагностика → образы; `/healthz` отвечает. Локализация (РФ-хост),
слой согласия + журналирование, дневная квота 20. Деплой: git push в Amvera, Python-режим, gunicorn.

## Итог

Реализован основной MVP + доп. фичи (ветка `feat/look-generator`, 8 коммитов): сквозной
продукт фото→диагностика→капсула→образы клиентки→шоп-лист+бюджет, агентный self-correction,
трекинг Identity Gap, eval-судья, веб-демо, приватный репо, тесты, security (0 находок).
Осталось до подачи (вне кода): ground-truth Ксении для полного eval, отзывы клиенток,
документы и презентация. Обязательное для «ИИ-стартап» (рабочий MVP + репозиторий) — закрыто.
