# План: База генерации образов (image-generation)

**Дата:** 2026-06-09
**Функция:** Справочник сборки фотореалистичного `image_generation_prompt` для preview-якоря квиза.

## Зачем

Preview — 2 бесплатных фото клиентки в её Формуле стиля — главный WOW-якорь покупки (mvp-spec). Поле `image_generation_prompt` в `formula-diagnostic` было, но без источника, как его собирать. Курс даёт структуру промпта и чек-лист фотореалистичности.

## Фазы

### [x] Фаза 1-2. Извлечение и база
- `reference/image-generation/image-generation.md` v1.0 + README.
- Структура промпта (6 блоков), фотореалистичный тулкит (камеры/плёнки/стиль), маппинг Формула→фотостиль, сохранение идентичности.

### [x] Фаза 3. Интеграция
- Пойнтер из `formula-diagnostic` (поле `image_generation_prompt` — 6-блочная структура, палитра без табу).
- Строка в `reference/README.md`, отметки в `KNOWLEDGE-BASE.md`.

### [ ] Фаза 4. Докрутка и валидация
- Углубить из «Разбор креативов», «Создание концепции», «Midjourney» (композиция, примеры) — 🟡.
- Привязать к конкретному image-API нашего стека (синтаксис параметров).
- Валидация: прогнать промпт на кейсе (Мишель/Анна), сверить реализм и сохранение идентичности.

## Итог

Реализован (фазы 1-3). База собрана и подключена к preview. Осталось: докрутка из визуальных уроков + привязка к API + валидация генерацией.
