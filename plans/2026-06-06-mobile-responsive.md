# План: мобильная адаптация сайта

**Дата:** 2026-06-06
**Функция:** довести мобильную версию статического сайта (`index.html`, `identity-scan-quiz.html`, `premium.html`) до состояния «красиво читается на всех телефонах» (320–430px).

## Контекст

Сайт уже в основном responsive (есть `@media`-брейкпоинты, viewport meta на всех страницах). Аудит на 320/360/375/390px через Playwright показал, что большая часть вёрстки на мобиле работает хорошо. Дефекты узкие и конкретные.

## Результаты аудита (2026-06-06)

| Страница | Состояние | Найденные дефекты |
|---|---|---|
| **identity-scan-quiz.html** | ✅ отлично | интро, экран вопроса, экран результата — без переполнений, читаемо. Работы не требует. |
| **premium.html** | 🟡 почти | `.footer-links` не переносятся → горизонтальный скролл ~6px на 320px. Шапка на мобиле = только логотип (ок). |
| **index.html** | 🔴 главный дефект | `.nav-cta` («Пройти диагностику») в шапке вылезает за край на **всех** телефонных ширинах (right ≈ 398px при vw 320–375) → горизонтальный скролл и обрезанная кнопка. Меню (`.nav-links`) на мобиле скрыто без замены — навигация недоступна. Hero и остальные секции на мобиле — хорошо. |

Подозрение по index `.answer` (FAQ, width 740px) — проверить, что это схлопнутый аккордеон (clip), а не реальный overflow.

## Фазы

### Фаза 1 · index.html — мобильная шапка
**Статус:** [x]
- [x] 1.1 Добавлена кнопка-бургер в шапку (видна только на мобиле, ≤900px).
- [x] 1.2 Добавлено выпадающее мобильное меню (`.nav-mobile`) с 5 пунктами навигации + CTA «Пройти диагностику».
- [x] 1.3 На мобиле скрыт `.nav-cta`, показан бургер. Горизонтальный скролл убран.
- [x] 1.4 JS-тоггл меню (open/close, aria-expanded, закрытие по клику на ссылку).
- [x] 1.5 FAQ `.answer` — overflow был артефактом схлопнутого аккордеона, реального скролла не даёт.

### Фаза 2 · premium.html — единообразие + футер
**Статус:** [x]
- [x] 2.1 `.footer-links` — `flex-wrap: wrap` + центрирование на ≤600px.
- [x] 2.2 Добавлен такой же бургер в шапку premium (тёмная тема, пункты ведут на index.html#…).

### Фаза 3 · Полировка
**Статус:** [x]
- [x] 3.1 Отступы/типографика на 320 и 430px проверены — тесных мест нет.
- [x] 3.2 Тач-таргеты бургера 44×44px.

### Фаза 4 · Перепроверка
**Статус:** [x]
- [x] 4.1 Прогон всех 3 страниц. Матрица (горизонтальный скролл):

| Страница | 320 | 360 | 375 | 430 |
|---|---|---|---|---|
| index | ✅ | ✅ | ✅ | ✅ |
| квиз (вкл. результат) | ✅ | ✅ | — | ✅ |
| premium | ✅ | ✅ | ✅ | ✅ |

Бургер-меню открывается/закрывается на index и premium, все пункты кликабельны.

## Метрики

| Метрика | Целевое значение | Чем меряем |
|---|---|---|
| Горизонтальный скролл на телефоне | 0 на всех ширинах 320–430 | `scrollWidth <= clientWidth` (Playwright) |
| Доступность навигации на мобиле | меню открывается, все 5 пунктов кликабельны | ручной прогон |
| Читаемость body-текста | ≥14px | аудит computed font-size |

## Итог

**Реализован целиком.**

**Сессия 2026-06-06:**
- Аудит через Playwright показал: квиз уже отлично адаптирован, premium почти (мелкий overflow футера), главный дефект — шапка index (CTA вылезал за край на всех телефонах, меню недоступно).
- index: добавлены бургер + выпадающее меню (5 пунктов + CTA), на мобиле скрыт `.nav-cta`. Горизонтальный скролл устранён.
- premium: добавлен бургер (тёмная тема), починен перенос `.footer-links`.
- Перепроверено на 320/360/375/430px — ноль горизонтального скролла на всех трёх страницах.

**Что осталось:** ничего по этому плану. На будущее — пункт 3.3 плана identity-lens (адаптив карточки архетипа) делается, когда появится сам Identity Lens.
